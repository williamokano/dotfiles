# Personal identity, stowed to ~/.gitconfig-personal.
# Loaded for repos under ~/Personal/ (see includeIf in .gitconfig).
[user]
	name = William Okano
	email = williamokano@gmail.com

[core]
	sshCommand = "ssh -i ~/.ssh/id_ed25519_pvt -o IdentitiesOnly=yes"
